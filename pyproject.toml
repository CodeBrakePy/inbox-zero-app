[project]
name = "inbox-zero-app"
version = "0.1.0"
description = "A local-first Python web app for managing inbox items."
readme = "README.md"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
